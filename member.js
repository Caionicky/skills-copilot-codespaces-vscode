function skillsMember() {
      // Get the member's skills
  const memberSkills = member.skills;
  // Get the member's name
  const memberName = member.name;
  // Get the member's age
  const memberAge = member.age;
  // Get the member's location
  const memberLocation = member.location;
  // Get the member's email
  const memberEmail = member.email;
  // Get the member's phone number
  const memberPhone = member.phone;
  // Get the member's bio
  const memberBio = member.bio;
  // Get the member's image
  const memberImage = member.image;
  // Get the member's social media
  const memberSocial = member.social;
  // Get the member's projects
  const memberProjects = member.projects;
  // Get the member's education
  const memberEducation = member.education;
  // Get the member's experience
  const memberExperience = member.experience;
  // Get the member's certifications
  const memberCertifications = member.certifications;
  // Get the member's languages
  const memberLanguages = member.languages;
  // Get the member's hobbies
  const memberHobbies = member.hobbies;

  return {
    memberSkills,
    memberName,
    memberAge,
    memberLocation,
    memberEmail,
    memberPhone,
    memberBio,
    memberImage,
    memberSocial,
    memberProjects,
    memberEducation,
    memberExperience,
    memberCertifications,
    memberLanguages,
    memberHobbies
  };
}